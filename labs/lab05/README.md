# Lab 5 —  Split + leakage checks

## 1. Задача

**Тип задачі:** А — класифікація текстів (тема/тональність/спам/urgency) 

---

## 2. Стратегія розділення даних

Для корпусу новин використано комбіновану стратегію split: `Group + Time-based`.
1. Дані групуються за категорією новин (category_id). 
2. Усередині кожної категорії новини сортуються за датою публікації (datePublished).
3. Після цього дані розділяються на:
* train — 70%
* validation — 15%
* test — 15%
Такий підхід дозволяє:
* уникнути temporal leakage (інформації з майбутнього у train);
* зберегти представлення всіх категорій у кожному спліті.

---

## 3. Leakage checks

1. Перевірка балансу класів
Таблиця кількостей
             train  val  test
category_id                  
0.0            147   31    32
1.0            144   31    32
2.0            144   30    32
3.0            144   30    32

2. Перевірка дублікатів
exact duplicates між сплітами: 0
near-duplicate pairs: 6

3. Template / metadata leakage (наявність у тексті назв класів або службових шаблонів: category, label, class, topic)
Підозрілі рядки з шаблонами: 0
Згадки назв класів у тексті: 2

4. Temporal leakage
Перевірено виконання умови: max(date_train) ≤ min(date_test)

---

## 4. Подальші покращення

* Перевірка знайдених near-duplicate пари та за потреби видалити або об’єднати дублікати.
* Очищення текстів від згадок назв категорій або інших підказок, які можуть створювати leakage.
* Додаткова перевірка метаданих джерел, щоб мінімізувати можливий source leakage.